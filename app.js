"use strict";
const a = 111;
//function test (x : number, y:number) : number { return x+y;}
const test = (x, y) => x + y;
const arr = ['123'];
const are = ['123', '123', '123'];
const obj = { lat: 1, lon: 3 };
const ztest = [
    '123'
];
class Addin {
    constructor(x, y) {
        this.a = x;
        this.b = Number.parseInt(y);
    }
}
const addin = new Addin(NaN, 'test');
// function alpha(x:number , y:number ) : number;
function alpha(x, y) {
    return x + y;
}
let q = 1;
q = 'hello';
try {
    const array = [];
}
catch (error) {
    error;
}
const head = document.querySelector("#head");
if (head) {
    const gas = head.innerHTML;
}
const typing = '123';
const weathercast = 'today weather is snow';
